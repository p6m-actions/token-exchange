name: Temp Action

on:
  pull_request:

jobs:
  temp:
    name: Create Release
    runs-on: ubuntu-latest

    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Get P6M YBOR Token
        id: token
        uses: ./
